# Example configuration for train_full.py
# You can override any of these by passing command-line arguments

data_dir: "data"
# For RTX 4060 8GB VRAM, 16GB RAM:
epochs: 30
batch_size: 64
lr: 0.0001
optimizer: "adam"
val_split: 0.2
checkpoint_dir: "models"
plot_dir: "results"
seed: 42
